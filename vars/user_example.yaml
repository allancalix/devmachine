# Email belonging to the user being initialized on the system.
user_email: "mygreatemail@example.com"
# Path to a dotfiles directory.
dotfiles_path: /home/superuser/sources/dotfiles
# Link generated ssh keys to GitHub account.
link_to_github: true
# Repos to clone into common locations.
repos:
  - repo: allancalix/dotfiles
    branch: main
